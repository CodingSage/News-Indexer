package edu.buffalo.cse.irf14.query;

public class ExpressionTree {

	private ExpressionNode head;

	public void addNode(String value) {
		if (head == null)
			head = new ExpressionNode(value);
		else {

		}
	}

	@Override
	public String toString() {
		return super.toString();
	}
}
